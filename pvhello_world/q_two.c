/* here the -c command is used for compilation to bring out an object code
 * used by the linker, this process goes through assemblier to bring out the
 * object code, but because it won't link to a new file destination,
 * The -o command is not needed */
gcc -c filename
