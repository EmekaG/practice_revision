/* here the -c command is used for compilation to bring out an object code
 * used by the linker, but because it won't link to a new file destination,
 * The -o command is not needed for the linker */
gcc -c filename
